/* Copy to config.js and fill in. The Firebase web config is from your Firebase console (Project settings → Your apps). */
window.AVA_CONFIG = {
  apiBase: '/voice-agent',
  allowedEmails: ['you@example.com'],
  firebase: {
    apiKey: 'YOUR_FIREBASE_WEB_API_KEY',
    authDomain: 'your-project.firebaseapp.com',
    projectId: 'your-project',
    appId: 'YOUR_FIREBASE_APP_ID',
  },
};
